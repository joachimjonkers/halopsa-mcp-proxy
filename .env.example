# Copy to .env for local testing (Claude Code registration passes these via -e instead).
# Both values come from your HaloPSA administrator — do not commit real values.

HALO_API_KEY=your-halo-api-key-here
HALO_URL=https://your-halopsa-instance/api/mcp
# HALO_TIMEOUT=60000
